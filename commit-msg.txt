Simplify AI App Generator UI - remove broken apiRoutes/uiComponents/pages sections

- Removed API Routes, UI Components, Pages sections from preview
- Only shows Models now (what the new generator returns)
- Cleaner flow: Generate → See Models → Launch App
- All structure property accesses now null-safe
